package parties;

/*import java.util.UUID;

import org.bukkit.Bukkit;
import org.bukkit.ChatColor;
import org.bukkit.scoreboard.*;*/

public class ScoreUpdater implements Runnable {

	@Override
	public void run() {
		/*for(UUID partyMember:partyList.getUserParty(player.getUniqueId()).Members){
			Score score = objective.getScore(ChatColor.GREEN + Bukkit.getPlayer(partyMember).getDisplayName()); //Get a fake offline player
			score.setScore(1);
		}*/
		
	}

}
